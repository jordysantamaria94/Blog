import { FilterSeriePipe } from './filter-serie.pipe';

describe('FilterSeriePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeriePipe();
    expect(pipe).toBeTruthy();
  });
});
